export class Urls {
    public static protocol = 'http'
    public static port = '5686'
    public static server = 'localhost'
    public static baseUrl = `${Urls.protocol}://${Urls.server}:${Urls.port}/`
}